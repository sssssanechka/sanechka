# Создаем список, который содержит два внутренних списка
a = [
    [1, 2, 3],
    [4, 5, 6],
]

# Добавляем число 7 в первый внутренний список
a[0].append(7)

# Удаляем первое число из второго внутреннего списка
a[1].pop(0)

# Считаем сумму чисел в каждом внутреннем списке и добавляем ее в конец списка
for sublist in a:
    sublist.append(sum(sublist))

# Выводим весь список
print(a)
